            phi.hat.strat.oos[t, k] <- ilogit(
                G0.tilde[k]
            )
            mu.hat.strat.oos[t, k] <- ilogit(
                B.tilde[1, k] + B.tilde[2, k] * x.hat[t]
            )
